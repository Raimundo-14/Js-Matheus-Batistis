let velocidade = 130;

if (velocidade > 80 && velocidade < 90) {
    console.log(`velocidade ${velocidade} km/h maior que a permitida, diminua a velocicade para 80 km/h`)
}else if (velocidade > 120) {
    console.log(`velocidade ${velocidade} km/h maior que a permitida, você foi multado diminua fi de rapariga pra 80 km/h`)
}else {
    console.log(`velocidade ${velocidade} km/h compativél com a via, boa viajem`)
}

